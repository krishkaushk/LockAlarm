# Webcam capture + frame processing
opencv-python>=4.8

# Face landmark detection (468 points per face)
mediapipe>=0.10

# Math for EAR distance calculations
numpy>=1.24

# Zero-latency audio playback — pre-decoded PCM, no subprocess
sounddevice>=0.4
soundfile>=0.12
